apiVersion: apps/v1
kind: Deployment
metadata:
  name: hape-dora-demo-service-w
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hape-dora-demo-service-w
  template:
    metadata:
      labels:
        app: hape-dora-demo-service-w
    spec:
      containers:
      - name: hape-dora-demo-service-w
        image: nginx:1.27
        ports:
        - containerPort: 80
